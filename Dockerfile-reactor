# ---- Build Stage ----
FROM eclipse-temurin:25-jdk-alpine AS build

WORKDIR /app

# Копируем только файлы Gradle для кэширования зависимостей
COPY gradlew .
COPY gradle gradle
COPY build.gradle .
COPY settings.gradle .
RUN chmod +x gradlew

# Скачиваем зависимости Reactor (кэшируется)
RUN ./gradlew :application-reactor:dependencies --no-daemon || true

# Теперь копируем исходный код
COPY common common
COPY loom loom
COPY reactor reactor
COPY application-loom application-loom
COPY application-reactor application-reactor

# Копируем сгенерированные jOOQ классы (с правильным путем)
COPY application-reactor/build/generated/sources/jooq/main application-reactor/build/generated/sources/jooq/main

# Собираем Reactor приложение
RUN ./gradlew :application-reactor:bootJar -x test -x generateJooq --no-daemon

# ---- Run Stage ----
FROM eclipse-temurin:25-jre-alpine

WORKDIR /app

# Копируем JAR
COPY --from=build /app/application-reactor/build/libs/*.jar app.jar

RUN addgroup -S appgroup && \
    adduser -S appuser -G appgroup && \
    mkdir -p /app/logs && \
    chown -R appuser:appgroup /app

USER appuser

EXPOSE 8080

ENTRYPOINT ["java", \
    "-XX:+UseZGC", \
    "-Xmx256m", \
    "-Xms128m", \
    "-XX:MaxMetaspaceSize=64m", \
    "-XX:+ExitOnOutOfMemoryError", \
    "-jar", "app.jar"]
